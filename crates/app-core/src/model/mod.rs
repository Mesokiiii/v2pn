//! Pure data types — `ProxyProfile` and the typed newtypes
//! (`TunInterfaceName`, `LoopbackPort`) that keep invalid values
//! unrepresentable at the public API boundary.

pub mod profile;
pub mod types;
